# ============================================================
# FinGuard AgentOps — Complete Requirements
# Python 3.11.x required
# ============================================================

# ── Core Agent Framework ─────────────────────────────────────
langgraph>=0.2.0
langchain>=0.3.0
langchain-anthropic>=0.2.0
langchain-openai>=0.2.0
langchain-chroma>=0.2.0
langchain-core>=0.3.0
crewai>=0.80.0                  # Optional: multi-agent coordination

# ── LLM Providers ───────────────────────────────────────────
anthropic>=0.39.0
openai>=1.50.0

# ── Document Processing ─────────────────────────────────────
PyMuPDF>=1.24.0                 # PDF parsing (import as fitz)

# ── Vector Store & RAG ───────────────────────────────────────
chromadb>=0.5.0
# weaviate-client>=4.0.0        # Alternative to ChromaDB

# ── Security Gateway ────────────────────────────────────────
nemoguardrails>=0.10.0          # NVIDIA NeMo Guardrails
guardrails-ai>=0.5.0            # Structured output validation
presidio-analyzer>=2.2.0        # PII detection
presidio-anonymizer>=2.2.0      # PII redaction
python-jose[cryptography]>=3.3.0 # JWT token handling (PyJWT alternative)

# ── API Framework ────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
python-dotenv>=1.0.0
python-multipart>=0.0.12        # File upload support
httpx>=0.27.0                   # Async HTTP client

# ── Observability & Instrumentation ─────────────────────────
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-exporter-otlp>=1.27.0
opentelemetry-exporter-prometheus>=0.48b0
opentelemetry-instrumentation-fastapi>=0.48b0
opentelemetry-instrumentation-requests>=0.48b0
prometheus-client>=0.21.0

# ── LLM Observability ───────────────────────────────────────
arize-phoenix>=5.0.0            # Embedding drift, hallucination tracking

# ── Experiment Tracking ─────────────────────────────────────
mlflow>=2.17.0

# ── Red Team & Evaluation ───────────────────────────────────
deepeval>=1.4.0                 # LLM evaluation metrics
# promptfoo installed via npm:  npm install -g promptfoo

# ── Testing ──────────────────────────────────────────────────
pytest>=8.3.0
pytest-asyncio>=0.24.0

# ── Utilities ────────────────────────────────────────────────
requests>=2.32.0
pyyaml>=6.0.0
